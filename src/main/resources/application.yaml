ktor:
  application:
    modules:
      - org.delcom.ApplicationKt.module

  deployment:
    port: 8000
    host: 0.0.0.0

  database:
    host: localhost
    port: 5432
    name: db_manajemen_data
    user: postgres
    password: Postgres

  jwt:
    secret: dev-secret